import React from "react";
import './SearchButton.css';

function SearchButton() {
  return <button type='submit' className="search-btn" id="search-btn">Search</button>;
};

export default SearchButton;